apiVersion: apps/v1
kind: Deployment
metadata:
  name: minirtos-frontend
  namespace: minirtos
spec:
  template:
    spec:
      containers:
        - name: frontend
          image: minirtos-playground-frontend:phase32
          imagePullPolicy: IfNotPresent
